# 1. Linux shell is a user interface present between user and kernel. It is used for executing commands
#    and communication with Linux OS. 
# 2. Linux shell is basically a program used by users for executing commands. 
# 3. It accepts human-readable commands as input and converts them into kernel understandable language. 

# Shell is a UNIX term for an interface between a user and an operating system service. 
# Shell provides users with an interface and accepts human-readable commands into the system and 
# executes those commands which can run automatically and give the program’s output in a shell script.
# A shell is accessed by a terminal which runs it.

# "#!" is an operator called shebang which directs the script to the interpreter location. 
# So, if we use "#! /bin/sh" the script gets directed to the bourne-shell.

#! /usr/bin/bash

echo "Hello World"